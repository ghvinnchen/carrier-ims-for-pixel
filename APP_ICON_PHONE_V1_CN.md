# Carrier IMS 手机图标版

本版仅替换 App Icon，UI 保持 V7 不变。

- 使用用户提供的蓝色手机图形；
- 按可见边界自动裁切并严格居中；
- Adaptive Icon 前景占比约 58%；
- 保留与之前一致的浅蓝背景；
- 同步更新普通图标、圆形图标、Android 13+ 单色主题图标及 Play Store 图标；
- 不包含 `.git`、`.gradle`、`.idea`、`build` 和本机 `local.properties`。

安装测试时建议先卸载旧版 App，再安装新 APK，以清除 Launcher 图标缓存。
