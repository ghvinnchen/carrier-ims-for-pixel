# Carrier IMS M3E V18.2 — Selected Icon Floating Tabs

基于 V18.1 Floating Pill Bottom Bar。

底部三个 Tab 的显示规则改为：

- 当前选中：显示 Icon + Tab 名称，并使用胶囊背景；
- 未选中：只显示 Tab 名称，不显示 Icon；
- 点击其他名称后，Icon 随选中状态移动到新的 Tab；
- 保留悬浮圆角容器、阴影和系统手势安全区。

效果参考用户提供的 Google Photos 底部导航交互。
