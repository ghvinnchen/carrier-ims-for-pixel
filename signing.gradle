/*
 * Signing is now configured safely in app/build.gradle.kts.
 *
 * This compatibility placeholder is intentionally kept so existing forks or
 * scripts that expect signing.gradle do not fail because the file is absent.
 * Do not apply this file from app/build.gradle.kts.
 *
 * Configure an optional custom key in local.properties (not committed):
 * SIGN_KEY_STORE_FILE=D:\\AndroidKeys\\carrierims.jks
 * SIGN_KEY_STORE_PASSWORD=your_store_password
 * SIGN_KEY_ALIAS=carrierims
 * SIGN_KEY_PASSWORD=your_key_password
 */
logger.lifecycle("CarrierIMS: signing.gradle compatibility placeholder loaded; signing is managed by app/build.gradle.kts.")
